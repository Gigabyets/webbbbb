<section class="space-y-12 pb-20">
    <header class="text-center max-w-4xl mx-auto pt-8">
        <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-yellow-600 mb-6">
            รวมค่ายสล็อตแตกง่าย อันดับ 1 ประจำปี 2024
        </h1>
        <p class="text-gray-300 text-lg leading-relaxed px-4">
            รีวิวเจาะลึกทุกค่ายเกมชั้นนำที่ได้รับความนิยมสูงสุดในไทย <strong>Fafawin168</strong> รวบรวมข้อมูลข้อดี-ข้อเสีย 
            และจุดเด่นของแต่ละค่ายเพื่อช่วยให้คุณตัดสินใจเลือกเล่นเกมที่เหมาะสมที่สุด พร้อมระบบ API แท้ 100%
        </p>
    </header>

    <div class="grid grid-cols-1 gap-12">
        <% providers.forEach((provider, index) => { %>
            <article id="<%= provider.id %>" class="scroll-mt-24 bg-gray-800/40 rounded-3xl border border-gray-700/50 overflow-hidden hover:border-yellow-500/50 transition-all duration-500 shadow-xl">
                <div class="flex flex-col lg:flex-row">
                    
                    <div class="lg:w-1/3 bg-gray-900/50 p-8 flex items-center justify-center border-b lg:border-b-0 lg:border-r border-gray-700/50">
                        <img src="<%= provider.img %>" 
                             alt="รีวิวค่ายเกม <%= provider.name %> สล็อตแตกง่าย" 
                             width="300" 
                             height="150"
                             class="max-h-48 object-contain filter drop-shadow-2xl"
                             <% if(index > 0) { %> loading="lazy" <% } %>
                             decoding="async">
                    </div>

                    <div class="lg:w-2/3 p-8 md:p-10 space-y-6">
                        <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
                            <h2 class="text-3xl font-bold text-white"><%= provider.name %></h2>
                            <div class="flex gap-2">
                                <span class="bg-yellow-500/10 text-yellow-500 text-xs font-bold px-3 py-1 rounded-full border border-yellow-500/20">สล็อตแตกหนัก</span>
                                <span class="bg-green-500/10 text-green-400 text-xs font-bold px-3 py-1 rounded-full border border-green-500/20">API แท้</span>
                            </div>
                        </div>

                        <p class="text-gray-300 leading-relaxed text-base">
                            <%= provider.desc %>
                        </p>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 pt-4">
                            <div class="bg-green-500/5 p-5 rounded-2xl border border-green-500/10">
                                <h3 class="text-green-400 font-bold mb-3 flex items-center gap-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                                    ข้อดีและจุดเด่น
                                </h3>
                                <ul class="space-y-2">
                                    <% provider.pros.forEach(pro => { %>
                                        <li class="text-gray-400 text-sm flex gap-2">
                                            <span class="text-green-500">•</span> <%= pro %>
                                        </li>
                                    <% }) %>
                                </ul>
                            </div>

                            <div class="bg-red-500/5 p-5 rounded-2xl border border-red-500/10">
                                <h3 class="text-red-400 font-bold mb-3 flex items-center gap-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
                                    ข้อควรระวัง
                                </h3>
                                <ul class="space-y-2">
                                    <% provider.cons.forEach(con => { %>
                                        <li class="text-gray-400 text-sm flex gap-2">
                                            <span class="text-red-500">•</span> <%= con %>
                                        </li>
                                    <% }) %>
                                </ul>
                            </div>
                        </div>

                        <div class="pt-4 flex flex-col sm:flex-row gap-4">
                            <a href="/register" class="flex-1 bg-yellow-500 hover:bg-yellow-400 text-black font-bold py-3 rounded-xl text-center transition shadow-lg shadow-yellow-500/10">
                                เล่นเกมค่ายนี้เลย
                            </a>
                            <button class="flex-1 border border-gray-600 hover:border-yellow-500 text-gray-300 hover:text-yellow-500 py-3 rounded-xl text-center transition">
                                ดูรีวิวเกมแตกง่าย
                            </button>
                        </div>
                    </div>
                </div>
            </article>
        <% }) %>
    </div>

    <footer class="bg-gray-800/20 p-8 rounded-3xl border border-gray-800 mt-16">
        <h2 class="text-2xl font-bold text-white mb-4">คำถามที่พบบ่อยเกี่ยวกับค่ายเกมสล็อต</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 text-gray-400 text-sm leading-relaxed">
            <div>
                <p class="font-bold text-yellow-500 mb-2">เล่นสล็อตค่ายไหนแตกดีที่สุด?</p>
                <p>จากการรวบรวมข้อมูลผู้ใช้ในปี 2024 ค่าย <strong>PG SOFT</strong> และ <strong>Pragmatic Play</strong> ยังคงครองอันดับ 1 ในเรื่องอัตราการจ่ายรางวัลที่สูงและฟีเจอร์ที่แตกหนักที่สุด</p>
            </div>
            <div>
                <p class="font-bold text-yellow-500 mb-2">ทำไมต้องเลือกเล่นผ่าน Fafawin168?</p>
                <p>เพราะเราใช้ระบบ API แท้ที่ดึงข้อมูลตรงจาก Server หลักของค่ายเกม ทำให้ไม่มีการปรับแต่งอัตราแพ้ชนะ และมีความเสถียรสูงที่สุดในเอเชีย</p>
            </div>
        </div>
    </footer>
</section>

<style>
    /* เพิ่ม Smooth Scrolling เฉพาะในหน้านี้ */
    html {
        scroll-behavior: smooth;
    }
    
    /* ตกแต่ง Scrollbar สำหรับการอ่านที่ลื่นไหล */
    article::-webkit-scrollbar {
        width: 4px;
    }
    article::-webkit-scrollbar-thumb {
        background: #4b5563;
        border-radius: 10px;
    }
</style>