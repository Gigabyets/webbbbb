<div class="space-y-12 pb-20">
    <header class="text-center space-y-4 pt-8">
        <h1 class="text-3xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-yellow-600">
            โปรโมชั่น fafawin168 เว็บเดิมพันออนไลน์
        </h1>
        <article class="max-w-3xl mx-auto px-4">
            <p class="text-gray-400 leading-relaxed text-sm md:text-base">
                รวมสุดยอดความคุ้มค่าที่ <strong>fafawin168</strong> มอบให้กับสมาชิกทุกคน เราคือ <h2 class="inline font-bold text-yellow-500 text-sm md:text-base">เว็บเดิมพันออนไลน์ที่มีโปรโมชั่นดีที่สุด</h2> ไม่ว่าคุณจะเป็นสายสล็อตหรือคาสิโน เลือกรับโบนัสที่ตอบโจทย์คุณได้ที่นี่ ทุกโปรโมชั่นถอนได้จริง มั่นคง 100%
            </p>
        </article>
    </header>

    <section id="recommended-promos" class="container mx-auto px-4 scroll-mt-20">
        <div class="flex items-center gap-3 mb-8 border-l-4 border-yellow-500 pl-4">
            <h3 class="text-2xl font-bold text-white">🔥 โปรโมชั่นแนะนำสำหรับคุณ</h3>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
            <% if (typeof promotions !== 'undefined') { %>
                <% promotions.forEach(promo => { %>
                    <article class="bg-gray-800 rounded-2xl overflow-hidden shadow-lg border border-gray-700 hover:border-yellow-500 transition duration-300 group cursor-pointer relative js-promo-card" 
                             data-promo='<%= JSON.stringify(promo) %>'>
                        <div class="aspect-[3/4] overflow-hidden relative">
                            <img src="<%= promo.img %>" alt="<%= promo.title %> - fafawin168" loading="lazy" class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent opacity-60"></div>
                        </div>
                        <div class="p-4 bg-gray-800/90 backdrop-blur-sm">
                            <h4 class="text-sm md:text-base font-bold text-white truncate"><%= promo.title %></h4>
                            <p class="text-xs text-yellow-500 mt-2 font-semibold flex items-center gap-1">
                                <span>ดูรายละเอียดเพิ่มเติม</span>
                                <span class="group-hover:translate-x-1 transition-transform">></span>
                            </p>
                        </div>
                    </article>
                <% }) %>
            <% } %>
        </div>
    </section>

    <section class="container mx-auto px-4 pt-10">
        <div class="bg-gradient-to-br from-gray-900 to-black border border-gray-800 rounded-3xl p-8 md:p-12">
            <div class="grid md:grid-cols-2 gap-10 items-center">
                <div class="space-y-6">
                    <h3 class="text-2xl md:text-3xl font-bold text-white">ทำไมต้องรับโปรโมชั่นที่ <span class="text-yellow-500">fafawin168</span>?</h3>
                    <article class="text-gray-400 text-sm md:text-base leading-relaxed space-y-4">
                        <p><strong>fafawin168 เว็บเดิมพันออนไลน์</strong> ของเราออกแบบโปรโมชั่นมาเพื่อเอื้อประโยชน์ต่อผู้เล่นมากที่สุด โดยยึดหลักความยุติธรรมเป็นสำคัญ:</p>
                        <ul class="space-y-3">
                            <li class="flex items-center gap-3"><span class="text-yellow-500 font-bold">✓</span> เงื่อนไขการทำเทิร์นต่ำที่สุดในเครือข่าย</li>
                            <li class="flex items-center gap-3"><span class="text-yellow-500 font-bold">✓</span> ระบบคำนวณโบนัสอัตโนมัติ แม่นยำ 100%</li>
                            <li class="flex items-center gap-3"><span class="text-yellow-500 font-bold">✓</span> ถอนเงินได้ทันทีหลังจากทำเงื่อนไขครบถ้วน</li>
                        </ul>
                    </article>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="bg-gray-800/50 p-6 rounded-2xl text-center border border-gray-700">
                        <div class="text-3xl mb-2 text-yellow-500">💰</div>
                        <p class="text-white font-bold text-sm">แจกหนักทุกวัน</p>
                    </div>
                    <div class="bg-gray-800/50 p-6 rounded-2xl text-center border border-gray-700">
                        <div class="text-3xl mb-2 text-yellow-500">⚡</div>
                        <p class="text-white font-bold text-sm">รับยอดไวใน 3 วิ</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

<div id="promoModal" class="fixed inset-0 z-[100] hidden" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
    <div class="fixed inset-0 bg-black/95 backdrop-blur-md" onclick="closePromoModal()"></div>
    <div class="fixed inset-0 z-10 overflow-y-auto">
        <div class="flex min-h-full items-center justify-center p-4">
            <div class="relative rounded-3xl bg-gray-900 border border-yellow-500 w-full max-w-4xl overflow-hidden animate-popup shadow-2xl">
                <button onclick="closePromoModal()" aria-label="ปิดหน้าต่าง" class="absolute top-4 right-4 z-30 bg-black/60 text-white rounded-full w-10 h-10 flex items-center justify-center hover:bg-red-500 transition">✕</button>
                
                <div class="max-h-[90vh] overflow-y-auto custom-scrollbar">
                    <div class="grid grid-cols-1 md:grid-cols-2">
                        
                        <div class="bg-black flex items-center justify-center min-h-[300px]">
                            <img id="modalImg" src="" alt="โปรโมชั่น fafawin168" class="w-full h-full object-contain md:object-cover">
                        </div>

                        <div class="p-6 md:p-10 flex flex-col bg-gray-900">
                            <h3 id="modalTitle" class="text-2xl md:text-3xl font-bold text-yellow-400 mb-4"></h3>
                            <div class="flex-grow">
                                <p id="modalDesc" class="text-gray-200 text-sm md:text-base mb-8 leading-relaxed"></p>
                                
                                <div class="bg-gray-800/50 rounded-2xl p-6 border border-gray-700">
                                    <h4 class="text-white font-bold mb-4 text-sm flex items-center gap-2">
                                        <span class="text-yellow-500">📜</span> เงื่อนไข fafawin168 เว็บเดิมพันออนไลน์
                                    </h4>
                                    <ul id="modalTerms" class="space-y-3 text-xs md:text-sm text-gray-400 list-disc list-inside leading-relaxed"></ul>
                                </div>
                            </div>

                            <div class="mt-8 flex gap-3 sticky bottom-0 bg-gray-900 pt-4 border-t border-gray-800/50">
                                <button onclick="closePromoModal()" class="flex-1 py-4 bg-gray-700 text-white rounded-xl font-bold hover:bg-gray-600 transition">ปิด</button>
                                <button onclick="window.location.href='/register'" class="flex-[2] py-4 bg-yellow-500 text-black rounded-xl font-bold shadow-lg hover:bg-yellow-400 transition transform active:scale-95">สมัครรับสิทธิ์ทันที</button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Modal Logic แบบเดียวกับหน้า Home เพื่อความเสถียร
    document.querySelectorAll('.js-promo-card').forEach(card => {
        card.addEventListener('click', function() {
            try {
                const data = JSON.parse(this.getAttribute('data-promo'));
                document.getElementById('modalImg').src = data.img || '';
                document.getElementById('modalTitle').textContent = data.title || '';
                document.getElementById('modalDesc').textContent = data.desc || '';
                
                const list = document.getElementById('modalTerms');
                list.innerHTML = data.terms && data.terms.length > 0 
                    ? data.terms.map(t => `<li>${t}</li>`).join('') 
                    : '<li>ติดต่อสอบถามข้อมูลเพิ่มเติมได้ที่พนักงานตลอด 24 ชั่วโมง</li>';
                
                document.getElementById('promoModal').classList.remove('hidden');
                document.body.style.overflow = 'hidden';
            } catch (e) {
                console.error("Promo Modal Error:", e);
            }
        });
    });

    function closePromoModal() {
        document.getElementById('promoModal').classList.add('hidden');
        document.body.style.overflow = '';
    }
</script>

<style>
    .custom-scrollbar::-webkit-scrollbar { width: 4px; height: 4px; }
    .custom-scrollbar::-webkit-scrollbar-thumb { background: #4b5563; border-radius: 10px; }
    @keyframes popupScale { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }
    .animate-popup { animation: popupScale 0.2s ease-out forwards; }
</style>