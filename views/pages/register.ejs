<div class="max-w-md mx-auto my-10 p-6 bg-gray-800 rounded-2xl border border-gray-700 shadow-2xl">
    <header class="text-center mb-8">
        <img src="/images/logo.webp" alt="FAFAWIN168" width="120" height="40" class="mx-auto mb-4 object-contain" decoding="async">
        <h1 class="text-3xl font-bold text-yellow-500 mb-2">สมัครสมาชิก</h1>
        <p class="text-gray-400 text-sm">กรอกข้อมูลเพื่อรับสิทธิพิเศษจากเรา</p>
    </header>

    <form action="#" class="space-y-4" onsubmit="event.preventDefault(); showLineAlert();">
        <div>
            <label for="phone" class="block text-sm font-medium text-gray-300 mb-1">เบอร์โทรศัพท์</label>
            <input type="tel" id="phone" name="phone" required
                   pattern="[0-9]{10}"
                   class="w-full bg-gray-900 border border-gray-700 rounded-lg py-3 px-4 text-white focus:outline-none focus:border-yellow-500 transition-colors" 
                   placeholder="0XXXXXXXXX">
        </div>
        
        <button type="submit" 
                class="w-full bg-yellow-500 text-black font-bold py-3 rounded-lg hover:bg-yellow-400 transition-all shadow-lg shadow-yellow-500/20 mt-6 active:scale-95">
            ยืนยันการสมัคร
        </button>
    </form>
</div>

<div id="linePopup" class="fixed inset-0 z-[110] hidden" role="dialog" aria-modal="true">
    <div class="fixed inset-0 bg-black/80 backdrop-blur-sm" onclick="closeLineAlert()"></div>
    
    <div class="fixed inset-0 flex items-center justify-center p-4 pointer-events-none">
        <div class="relative transform rounded-2xl bg-gray-900 border border-yellow-500 p-8 text-center shadow-2xl max-w-sm w-full pointer-events-auto animate-popup">
            <div class="text-5xl mb-4">💬</div>
            <h2 class="text-2xl font-bold text-white mb-2">สมัครสมาชิกสำเร็จ!</h2>
            <p class="text-gray-300 mb-6 text-sm">
                เพื่อรับยูสเซอร์และรหัสผ่านเข้าเล่น<br>
                <span class="text-yellow-500 font-bold text-lg">กรุณาแอดไลน์เจ้าหน้าที่</span>
            </p>
            
            <div class="space-y-3">
                <a href="https://line.me/ti/p/@yourlineid" target="_blank" rel="noopener" 
                   class="block w-full bg-[#06C755] text-white font-bold py-3 rounded-lg hover:opacity-90 transition-opacity flex items-center justify-center gap-2">
                    <span>LINE : @fafawin168</span>
                </a>
                <button type="button" onclick="closeLineAlert()" class="block w-full bg-gray-700 text-gray-300 py-2 rounded-lg text-xs font-medium hover:bg-gray-600 transition-colors">
                    ปิดหน้าต่าง
                </button>
            </div>
        </div>
    </div>
</div>

<script>
    /**
     * ฟังก์ชันแสดงป๊อปอัพ
     */
    function showLineAlert() {
        const popup = document.getElementById('linePopup');
        popup.classList.remove('hidden');
        document.body.style.overflow = 'hidden'; // ล็อคสก์โรลพื้นหลัง
    }

    function closeLineAlert() {
        const popup = document.getElementById('linePopup');
        popup.classList.add('hidden');
        document.body.style.overflow = ''; // คืนค่าการสก์โรล
    }

    // ปิดด้วย ESC
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeLineAlert();
    });
</script>

<style>
    /* CSS Animation ประสิทธิภาพสูง */
    @keyframes popupScale {
        from { opacity: 0; transform: translateY(10px) scale(0.95); }
        to { opacity: 1; transform: translateY(0) scale(1); }
    }
    .animate-popup {
        animation: popupScale 0.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        will-change: transform, opacity;
    }
</style>